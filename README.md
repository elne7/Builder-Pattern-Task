# Builder_pattern_task

A new Flutter task implements the `Builder` design pattern.

This task shows a listview widget that displays some information about a user using the builder method of the listview
